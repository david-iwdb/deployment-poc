# Normalize line endings to LF for all text files so formatting is
# consistent across Windows, macOS, and Linux (and CI).
* text=auto eol=lf

# Binary/generated files git should not touch
pnpm-lock.yaml -diff
